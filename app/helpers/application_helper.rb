module ApplicationHelper
  # def error_message_for(object, field, options = {})
  #   if object.errors[field].present?
  #     content_tag(:div, object.errors[field].first, options)
  #   end
  # end
end
